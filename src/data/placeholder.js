/**
 * Demo mode. TRUE until every value marked `placeholder: true` has been
 * replaced with something real and confirmed by the board.
 *
 * While true, invented content renders with a visible amber marker so nobody
 * mistakes a guess for fact. Set false before this site is ever public.
 */
export const DEMO = true;